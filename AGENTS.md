# Vi$ion public repository

Preserve the proprietary calculator boundary. Keep the hosted calculator adapter and public API route. Never copy calculator implementation, private fixtures, credentials, deployment archives or Site history into this repository.

Use docs/VERSIONING.md and release-manifest.json for source provenance. Future GitHub updates require explicit owner approval; existing authorization in the active session remains valid. Do not treat reminders as automatic sync permission. Do not force-push. Report test/build results and any unverified browser or hosted-service behavior.
